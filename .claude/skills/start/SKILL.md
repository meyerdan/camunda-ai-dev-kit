---
name: start
description: Start a Camunda process instance with variables. Use when testing or launching a process.
argument-hint: "[process id and variables]"
---

Start a process instance. $ARGUMENTS

Use c8ctl: `c8 create pi` or `c8 run` (deploy + start in one shot).
Run `c8 help create` for flags.
