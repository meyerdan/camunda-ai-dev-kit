---
name: status
description: Check Camunda process instance status, user tasks, and incidents. Use when monitoring or debugging processes.
argument-hint: "[filter criteria]"
---

Check Camunda status. $ARGUMENTS

Use c8ctl to list instances, search incidents, get instance details.
Run `c8 help search` and `c8 help list` for available filters.
